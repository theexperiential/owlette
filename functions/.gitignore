# Compiled output (top-level only — src/lib/ holds source modules)
/lib/
/lib-test/

# Dependencies
node_modules/

# Local env files (block all .env* — including project-specific overlays
# like .env.owlette-prod-90a12 — but keep the committed example template)
.env*
!.env.example

# IDE
.idea/
*.swp
*.swo
