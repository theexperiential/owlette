# api reference (legacy)

this page has been superseded by [the public api reference](../api/reference.md), which is rendered from `web/openapi.yaml` and exposed at `/api/openapi`.

use `docs/api/reference.md` for the current public api surface.
