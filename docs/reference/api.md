# api reference (legacy)

this page has been superseded by the public api reference at `/docs/api/reference`, which is rendered from `web/openapi.yaml` and exposed at `/api/openapi`.

use `web/content/docs/api/reference.mdx` for the current public api surface. the `docs/api/`
directory this page used to point into no longer exists — its consumer pages moved to fumadocs
in the 2026-05-20 migration, and its five unpublished launch/preview pages moved to
`docs/internal/` on 2026-09-05.
