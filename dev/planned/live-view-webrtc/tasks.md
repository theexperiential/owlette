# live-view-webrtc — tasks

**Progress**: 0/0 (planning placeholder)

This file is a stub. When the feature is prioritized, populate with concrete
tasks per the wave structure in [plan.md](plan.md). Until then, no tasks
should be picked up — the only artifact is the cli stub at
[cli/src/commands/machine.ts:432](../../../cli/src/commands/machine.ts#L432)
pointing here.

## log

### 2026-04-26

- Plan created. Spun out of `dev/completed/api-sprint/` Wave 4 (originally an
  SSE-based spike; reframed around WebRTC as the more appropriate transport
  for low-latency video). Live-view cli stub updated to point at this plan
  instead of the api-sprint W4 reference.
