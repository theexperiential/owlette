pywin32==306
psutil==5.9.5
pystray==0.19.5
email-validator==2.3.0
GPUtil==1.4.0
cryptography>=44.0.0,<45.0.0
google-auth==2.56.2
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
customtkinter==5.2.2
CTkListbox==0.9
CTkMessagebox==2.7
CTkToolTip==0.9
packaging==23.1
setuptools==83.0.0  # >=78.1.1 fixes PackageIndex.download path traversal; >=83.0.0 fixes the MANIFEST.in sdist bypass
requests==2.34.2  # >=2.32.0 .netrc leak, >=2.32.4 verify=False reuse, >=2.33.0 extract_zipped_paths temp reuse
tzlocal==5.4.4  # Convert Windows registry tz names to IANA (e.g. "Pacific Standard Time" -> "America/Los_Angeles") for dashboard timezone display

# Screenshot capture
mss==9.0.2  # Fast cross-platform screenshot library

# Temperature monitoring
WinTmp==1.3.0  # CPU and GPU temperature (LibreHardwareMonitor wrapper)
nvidia-ml-py==12.560.30  # NVIDIA GPU fallback (NVML bindings)

# Cortex AI agent (local LLM-powered investigation + chat)
claude-agent-sdk>=0.1.0,<2.0.0

# Error monitoring (optional — only active when DSN configured in config.json)
sentry-sdk>=2.0.0,<3.0.0

# Firebase integration - now using REST API with OAuth tokens (no Admin SDK needed)
# firebase-admin==6.4.0  # REMOVED - replaced with REST API client
# google-cloud-firestore==2.14.0  # REMOVED - replaced with REST API client